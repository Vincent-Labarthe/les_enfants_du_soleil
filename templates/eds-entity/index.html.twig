{% extends 'base.html.twig' %}
{% block body %}
    <div class="d-flex flex-row">
        {% include '_include/_sidebar.html.twig' %}
        <div class="container hero-container mt-5 p-5">
            <div class="row mb-5">
                <div class="col-md-6">
                    <div>
                        <h1 class="hero-title">{{ eds_entity.name }}</h1>
                    </div>
                </div>
                <div class="col-md-6 text-end">
                    <a href="{{ path('app_eds_entity_add') }}"><button class="btn btn-primary">Ajouter un site EDS</button></a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}