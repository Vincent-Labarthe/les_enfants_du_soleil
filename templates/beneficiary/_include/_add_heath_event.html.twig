<div class="table-design" id="localisation_table">
    <h4 class="mb-4">Ajouter un évènement de santé</h4>
    {{ form_start(form, {'action': path('app_beneficiary_health_add_ajax', {'id': person.id} )}) }}
    {{ form_widget(form) }}
    <button onclick="" class="btn btn-primary mb-5 text-end btn-eds">Créer</button>
    {{ form_end(form) }}
</div>
