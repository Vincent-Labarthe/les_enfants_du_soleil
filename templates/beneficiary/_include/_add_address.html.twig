<div class="table-design" id="localisation_table">
    <h4 class="mb-4">Ajouter une adresse</h4>
    {{ form_start(form, {'action': path('app_beneficiary_address_add_ajax', {'id': person.id} )}) }}
    {{ form_widget(form) }}
    <button onclick="" class="btn btn-primary mb-5 text-end">Créer</button>
    {{ form_end(form) }}
</div>

